<% partial_path = "pages/#{@page_name}" %>

<% content_for :page_header, @title %>
<% breadcrumb @breadcrumb %>

<h2>Articles Title</h2>
<%= render partial: 'editable/text', locals: { phrase: "#{Article::SIZE_SETTING_PREFIX}/articles/title/size" } %>
<%= link_to 'View Page Example', articles_path %>

<h2>Articles body</h2>
<%= render partial: 'editable/text', locals: { phrase: "#{Article::SIZE_SETTING_PREFIX}/articles/body/size" } %>
<%= link_to 'View Page Example', articles_path %>

<hr>
<h2>Article Title</h2>
<%= render partial: 'editable/text', locals: { phrase: "#{Article::SIZE_SETTING_PREFIX}/article/title/size" } %>
<%= link_to 'View Page Example', article_path(Article.all.last) if Article.all.last.present? %>

<h2>Article body</h2>
<%= render partial: 'editable/text', locals: { phrase: "#{Article::SIZE_SETTING_PREFIX}/article/body/size" } %>
<%= link_to 'View Page Example', article_path(Article.all.last) if Article.all.last.present? %>

<p>Note: Example links for Article pages may error if there are no articles.</p>